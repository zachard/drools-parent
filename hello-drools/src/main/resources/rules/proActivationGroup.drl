package com.pro

rule "rule.pro.activation.group.01"
    activation-group "group.01"
    when
        eval(true);
    then
        System.err.println("rule.pro.activation.group.01执行了");
end

rule "rule.pro.activation.group.02"
    activation-group "group.01"
    when
        eval(true);
    then
        System.err.println("rule.pro.activation.group.02执行了");
end